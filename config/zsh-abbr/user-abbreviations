abbr "bud"="brew update"
abbr "bug"="brew upgrade"
abbr "c."="code ."
abbr "cbcp"="fzf | xargs cat | pbcopy"
abbr "dc"="docker-compose"
abbr "dcdown"="docker-compose down"
abbr "dcps"="docker-compose ps"
abbr "dcup"="docker-compose up"
abbr "dex"="docker exec -it"
abbr "dimg"="docker images"
abbr "dlog"="docker logs"
abbr "dlogf"="docker logs -f"
abbr "dps"="docker ps"
abbr "dpsa"="docker ps -a"
abbr "drm"="docker rm"
abbr "drmi"="docker rmi"
abbr "drun"="docker run"
abbr "dstop"="docker stop"
abbr "ga"="git add"
abbr "gap."="git add -p ."
abbr "gap"="git add -p"
abbr "gb"="git branch"
abbr "gbd"="git branch -d"
abbr "gc-a"="git commit --amend"
abbr "gc"="git commit -m \"%\""
abbr "gcamm"="git commit --amend"
abbr "gco-fzf"="git branch --all | fzf | cut -c 3- | xargs git checkout"
abbr "gco"="git checkout"
abbr "gcob"="git checkout -b"
abbr "gcop"="git checkout -"
abbr "gd"="git diff"
abbr "gdc"="git diff --cached"
abbr "gf"="git fetch"
abbr "gl"="git log"
abbr "gl1l"="git log --oneline"
abbr "gll"="git log --oneline -10"
abbr "gm-fzf"="git branch --all | fzf | cut -c 3- | xargs git merge"
abbr "gm"="git merge"
abbr "gp-f"="git push --force"
abbr "gp-fwl"="git push --force-with-lease"
abbr "gp"="git push"
abbr "gpl"="git pull"
abbr "gr-c"="git rebase --continue"
abbr "gr-i"="git rebase -i"
abbr "gr"="git rebase"
abbr "gsl"="git stash list"
abbr "gsp"="git stash pop"
abbr "gss"="git stash save"
abbr "gst"="git status"
abbr "h"="history"
abbr "l"="ls -CF"
abbr "la"="ls -la"
abbr "mk"="mkdir -p"
abbr "v"="nvim"
